import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clarityfinancialtools.app',
  appName: 'Clarity',
  webDir: 'dist/clarity-frontend/browser'
};

export default config;
