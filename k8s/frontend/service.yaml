apiVersion: v1
kind: Service
metadata:
  name: clarity-frontend
  namespace: clarity
  labels:
    app: clarity-frontend
spec:
  selector:
    app: clarity-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
