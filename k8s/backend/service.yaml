apiVersion: v1
kind: Service
metadata:
  name: clarity-api
  namespace: clarity
  labels:
    app: clarity-api
spec:
  selector:
    app: clarity-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
