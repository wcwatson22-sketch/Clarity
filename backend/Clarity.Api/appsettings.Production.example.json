{
  "_comment": "EXAMPLE FILE — safe to commit. Copy to appsettings.Production.json and fill in your values. All secrets (JWT key, SMTP password, Stripe keys, VAPID keys) must be set as environment variables — never in this file.",

  "AppUrl": "https://clarityfinancialtools.com",

  "Admin": {
    "Username": "your_admin_username_here"
  },

  "AllowedOrigins": [
    "https://clarityfinancialtools.com",
    "capacitor://localhost",
    "http://localhost"
  ],

  "_corsNote": "capacitor://localhost and http://localhost are required for the iOS/Android app wrapper. Do NOT add wildcards or AllowAnyOrigin here.",

  "Smtp": {
    "Host": "smtp.resend.com",
    "Port": 587,
    "Username": "resend",
    "FromEmail": "noreply@clarityfinancialtools.com",
    "FromName": "Clarity Finance"
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Warning",
      "Override": {
        "Microsoft": "Error",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}
